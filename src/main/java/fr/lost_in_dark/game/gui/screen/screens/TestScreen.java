package fr.lost_in_dark.game.gui.screen.screens;

import fr.lost_in_dark.game.gui.screen.Screen;
import fr.lost_in_dark.game.render.MatrixStack;

public class TestScreen extends Screen {

    @Override
    public void render(MatrixStack stack, double x, double y) {
        //stack.push();
        //stack.scale(10.0F);
        //Gui.getInstance().getFont().render(stack, "test", 0, 0);
        //stack.pop();

        //RenderUtil.drawRect(stack, 10, 10, 200, 200, Color.RED.getRGB());
    }
}
